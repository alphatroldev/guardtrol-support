export enum ModificationStatus {
  Pending = "pending",
  Approved = "approved",
  Reverted = "reverted",
  NoResponse = "no-response",
}

export enum GuardStatus {
  OnDuty = "on duty",
  OffDuty = "off duty",
}

export enum BooleanEnum {
  True = "true",
  False = "false",
}
